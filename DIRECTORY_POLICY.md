# Directory Policy

Every directory must have one responsibility.

No business logic inside the Kernel.

No duplicated functionality.

No hidden dependencies.

No undocumented directory.

Every directory contains its own README.

Every directory has an owner.

Every directory is versioned.
